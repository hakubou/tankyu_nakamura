# 振子エンコーダ 仕様検討への回答

2026/08/17 メンバーB → メンバーA

いただいた3点について、シミュレーションで数字を出しました。結論から書くと
**分解能は現物で十分、ジッタは思ったより緩い制約、効くのは遅延**です。

以下の数値はすべて、本日実測したアーム慣性 `J_r = 2.13e-3 kg·m²` を反映した
モデルに基づいています。この値は従来の概算の 5.3 倍で、制御権限が 5 分の 1 に
落ちているため、8/14 以前の検討結果からは条件が変わっています。

---

## ① 分解能：E8T-512（0.176°/count）で十分です

α の分解能だけを振って、倒立時のリミットサイクル振幅を見ました。

| 分解能 | 1カウント | リミットサイクル |
|---|---|---|
| 理想（量子化なし） | — | 2.07° |
| 8192 count/rev | 0.044° | 2.04° |
| 4096 count/rev | 0.088° | 2.02° |
| **2048 count/rev ← 現物** | **0.176°** | **2.07°** |
| 1024 count/rev | 0.352° | 2.14° |
| 512 count/rev | 0.703° | 2.12° |
| 256 count/rev | 1.406° | 2.47° |

理想と現物で差がありません。**8倍粗い 256 count/rev でも成立します。**

### なぜ効かないか

律速しているのは分解能ではなく**クーロン摩擦 0.126 N·m** です。

α の 1 LSB がトルク指令に与える揺れは

```
K[1] × LSB = 12.81 × 3.07e-3 = 0.039 N·m
```

で、クーロン摩擦の 3 分の 1 以下。さらにオブザーバが平滑化します。
測定を細かくしても、摩擦由来の 2° 程度のリミットサイクルは減りません。

**→ E8T-512 のままで問題なし。より細かいエンコーダに投資する意味はありません。**

---

## ② 送信周期：等間隔は必須条件ではありません。効くのは遅延です

### 前提の訂正

「タイミングジッタが量子化誤差の7倍」という数字は、私が **モータ側の CAN
フィードバック** について出したものです。そこでの結論は「ジッタをゼロにせよ」
ではなく「**オブザーバ設計時に観測雑音へジッタ分を計上せよ**」でした。

実際、観測雑音を量子化誤差だけで見積もっていたために、オブザーバゲイン L が
2.4e5 まで暴れて全ケース転倒したのが本日の詰まりどころです。ジッタ分を
入れたら L は 6.2e3 に下がり、全ケース成立しました。

### ジッタと遅延は効き方が違う

- **ジッタ（標本化時刻のばらつき）** … 観測雑音として現れる。カルマンフィルタが吸収する
- **遅延（純粋な時間遅れ）** … 位相遅れとして現れる。**吸収できない**

数値で確認しました。オブザーバは公称設計のまま固定しています。

**ジッタ耐性**

| σ_t | 結果 | リミットサイクル |
|---|---|---|
| 0.5 ms | 成立 | 2.09° |
| 1.0 ms | 成立 | 2.24° |
| 2.0 ms | 成立 | 1.95° |
| 5.0 ms | 成立 | 2.66° |

**遅延耐性**

| 伝送遅延 | 結果 |
|---|---|
| 20 ms | 成立 |
| 30 ms | 成立 |
| **40 ms** | **成立（限界）** |
| 50 ms | ★転倒（0.25 s） |
| 60 ms 以上 | ★転倒（0.10 s） |

**破綻点は 40〜50 ms の間**で、不安定極の時定数 90 ms の約半分です。
ジッタは 5 ms でも平気ですが、遅延は 50 ms で落ちます。

### 提案する仕様

| 項目 | 値 | 根拠 |
|---|---|---|
| 送信レート | **1 kHz** | 制御周期 3.33 ms に対し常に 1 ms 以内の新しい値がある状態にする |
| 遅延 | **10 ms 以下** | 破綻点 40〜50 ms に対し 4〜5 倍の余裕 |
| ジッタ | 2 ms 以下（努力目標） | 守れなくても致命傷にならない |
| タイムスタンプ | **付ける** | 下記参照 |
| サンプリング | **ハードウェアタイマ割り込み** | `loop()` 内だとタイムスタンプが実態を表さない |

300 Hz に同期させる必要はありません。同期機構のほうが高くつきます。

### タイムスタンプを付ける理由

1. PC 側で `α̇ × 経過時間` の外挿ができ、**受信側ジッタの影響を原理的に消せる**
2. 遅延とジッタを**推測ではなく実測**できる。これがオブザーバ設計の入力そのもので、
   今日詰まった原因がまさにここ
3. サンプル落ちと通信途絶をウォッチドッグで検出できる

コストは 4 バイトです。

なお**マイコンと PC の時計は同期しません**。タイムスタンプは相対間隔と鮮度の
判定にのみ使い、絶対時刻を合わせようとしないでください。

### 帯域

1 kHz × 13 バイト = **13 kB/s**。

- ネイティブ USB CDC … 余裕（~1 MB/s）
- UART ブリッジ 115200 baud … **11.5 kB/s で足りません**

921600 baud 以上か、ネイティブ USB を使ってください。

---

## ③ 単位：生カウントで送ってください

メンバーAさんの見立てのとおりです。理由を挙げます。

1. **整数で厳密。** 丸めも浮動小数の書式も入りません。度で送ると 0.176° を
   表すのに小数3桁が必要になります

2. **ゼロ点は PC が持つべき。** α=π のゼロは「垂らして静止させる」校正で決まり、
   毎回取り直す可能性があります。マイコンが度で出すと、自分が持っていない
   ゼロ点を暗黙に主張することになります。E8T にはインデックス（Z相）が無いので
   なおさらです

3. **多回転に耐える。** スイングアップ中に振子は何回転もします。
   **int32 の累積カウント**で送ってください（**剰余は取らない**）。
   2048 count/rev なら int32 で ±100万回転あり、実質ラップしません。
   剰余で送ると PC 側でアンラップ処理が必要になり、故障モードが1つ増えます。
   モータ側の 16bit エンコーダで `Motor.UnwrapDelta()` を書く羽目になったのと
   同じ話で、繰り返す必要はありません

4. **CPR が変わっても PC 側の定数1個**で済みます

5. **切り分けができる。** 振子をちょうど1回転させて 2048 が出るか確認できます。
   1024 しか出なければ4逓倍が2逓倍になっていると即座に分かります

### フレーム案（リトルエンディアン、13 バイト）

| フィールド | 型 | 用途 |
|---|---|---|
| sync | uint16 = 0xA5A5 | 同期 |
| seq | uint16 | 連番。欠落検出 |
| t_us | uint32 | マイコン側マイクロ秒 |
| count | int32 | 累積カウント（4逓倍・符号付き） |
| crc8 | uint8 | 化け検出 |

立ち上げ時だけテキスト出力に切り替えられるフラグがあると楽だと思います。

---

## 補足

### STM32G474RE なら TIM2 か TIM5 を使ってください

どちらも **32bit カウンタ**のエンコーダモードを持ちます。ハードウェアが
累積カウントをそのまま保持するので、③ の「int32 累積」という要件が
**オーバーフロー処理なしで自動的に満たせます**。

ESP32 の PCNT は 16bit でソフト側のオーバーフロー処理が要るため、
STM32 に移ったことで条件が良くなっています。TI12 両エッジで4逓倍です。

### レベル変換の確認

E8T は 5V シングルエンド出力です。G474 の該当ピンが 5V トレラント（FT）で
あればレベルシフタを省ける可能性があります。データシートでピンごとに
確認してください。判断がつかなければレベルシフタを入れたままで構いません。

### CLAUDE.md の記述が古い

振子エンコーダ側のマイコンを ESP32 + PCNT と書いてありますが、実際は
STM32G474RE です。こちらで更新しておきます。

---

## まとめ

| 論点 | 結論 |
|---|---|
| 分解能 | E8T-512（0.176°）で十分。8倍粗くても成立。変更不要 |
| 送信レート | 1 kHz。300 Hz への同期は不要 |
| 遅延 | **10 ms 以下**。これが唯一の厳しい要件（破綻点 40〜50 ms） |
| ジッタ | 2 ms 以下が望ましいが、5 ms でも成立する |
| タイムスタンプ | **付ける**。実測値がオブザーバ設計の入力になる |
| 単位 | **生カウント（int32 累積、剰余を取らない）** |
| ペリフェラル | TIM2 または TIM5（32bit エンコーダモード） |
