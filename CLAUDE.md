# プロジェクト方針

## 概要

Damiao **DM-J4310-2EC V1.1** を用いた **回転型倒立振子（Furuta pendulum）** の製作。

- 期間: 2026/08/10（月）〜 2026/08/27（木）、月・水・木の週3回 × 3週 = 全9回
- 詳細計画は [`倒立振子_ロードマップ.md`](倒立振子_ロードマップ.md) を参照
- 現状: 速度モードでの回転を実機確認済み（2026/08/06）。機構部品は未入手

---

## 言語・実装方針

### **設計・実装は C# で行う**

**新規に書くコードは原則すべて C#（.NET 10）**とする。制御ループ、状態推定、
LQR、スイングアップ、ログ出力まで C# 側に実装する。

- ターゲット: `net10.0`
- `ImplicitUsings` / `Nullable` はともに **enable**
- 既存の `DamiaoCan` ライブラリを土台に拡張する（ゼロから書き直さない）
- テストは `DamiaoCan.Tests`（`FakeCanBus` で実機なしに検証できる構成）に追加する

### Python の扱い

`damiao_can.py` / `demo_1回転.py` は 2026/08/06 の動作検証で使った**リファレンス実装**。
C# 版はこれを移植したもの。**Python 側は今後更新しない**。
挙動に疑問が出たときの照合用としてのみ残す。

### 例外: モデル導出とゲイン計算

Furuta の運動方程式の導出（SymPy）と LQR / カルマンゲインの事前計算は、
**Python でオフラインに行ってよい**。手計算では Coriolis 項の符号を必ず誤るため。

ただし**成果物は数値行列（ゲイン `K`、オブザーバゲイン `L`、`A`, `B`）であり、
それを C# のソースに定数として埋め込む**。実行時に Python を呼ぶ構成にはしない。

### 振子角センサ（QUBE-Servo 2 内蔵の E8P）

ESP32 のファームは C# では書けないため C++（Arduino）で実装する。
役割は「E8P を PCNT で直交デコードして USB シリアルに垂れ流す」だけに限定し、
**制御ロジックは一切載せない**。PC 側（C#）が最新値を読む。

---

## 既存資産

```
モータ制御プログラム一式_20260806/
├── README.md                 動作検証の手順書・つまずきポイント集（必読）
├── damiao_can.py             Python リファレンス実装（更新しない）
├── demo_1回転.py
└── csharp/
    ├── DamiaoCan.slnx
    ├── src/
    │   ├── DamiaoCan/        ライブラリ本体
    │   │   ├── CanFrame.cs        CANフレーム（readonly struct）
    │   │   ├── ICanBus.cs         バス抽象（テスト差し替え用）
    │   │   ├── SeeedCanBus.cs     USB-CAN Analyzer V8.00 のシリアルプロトコル実装
    │   │   ├── Motor.cs           モータ制御本体
    │   │   ├── MotorFeedback.cs   フィードバックフレームの解釈
    │   │   ├── MotorScaling.cs    MIT の値域と実数⇔整数変換
    │   │   └── ControlMode.cs     制御モード enum
    │   └── DemoOneTurn/      1回転デモ
    └── tests/DamiaoCan.Tests/
        ├── FakeCanBus.cs     実機なしでテストするための偽バス
        ├── MotorCommandTests.cs / MotorScalingTests.cs
        ├── MotorFeedbackTests.cs / SeeedProtocolTests.cs
```

**実装済みで再利用するもの**

- MIT フレームの組み立て … `Motor.BuildMitFrame()`（単体テスト済み）
- フィードバック解釈 … `MotorFeedback.Decode()`
- 値域変換 … `MotorScaling.FloatToUInt()` / `UIntToFloat()`
- 使能／失能、制御モードの読み書き＋フラッシュ保存 … `Motor.Enable/Disable/ReadMode/SetMode`
- USB-CAN アダプタのシリアルプロトコル … `SeeedCanBus`
- 16bit エンコーダのラップアラウンド補正 … `Motor.UnwrapDelta()`

---

## ハードウェア構成

| 項目 | 内容 |
|---|---|
| モータ | DM-J4310-2EC V1.1、**CAN ID = 4** |
| 定格 | 24V / 2.5A、ピーク 7.5A、過電流保護 9.8A 以下 |
| トルク | 定格 3 N·m、ピーク 7 N·m、減速比 10:1 |
| 電源 | OMRON S8FS-G24024（240W級） |
| CAN 変換 | USB-CAN Analyzer V8.00（120Ω終端内蔵）、CH340 で仮想COMとして見える |
| 通信 | CAN **1 Mbps** 標準フレーム / PC-アダプタ間シリアル **2 Mbps** |
| PC | Windows 11、既定ポート `COM3` |
| 配線 | XT30(2+2)。GND は電源側と USB-CAN 側で共通にすること |

**制御モードは MIT(1) に変更済み・フラッシュ保存済み**（2026/08/10、電源再投入で定着確認）。

`MotorScaling` の定数は実レジスタと照合済み（`PositionMax=12.5` / `VelocityMax=30.0` /
減速比 10:1）。`TorqueMax` は当初 10.0 だったが、**2026/08/12 に 2.0 へ変更済み**
（下記「T_MAX を下げる」参照）。`Motor.ReadParameterSingle(RegisterId.TorqueMax)` で
CAN 経由でいつでも読み直せる。公式上位機（達妙調試助手）は入手できていないが、これで代替できる。

### 振子部：Quanser QUBE-Servo 2 回転振子モジュール一式（磁石台座付き）

エンコーダは **US Digital E8P-512-118**（シングルエンド、4ピン CON-MIC4）。

| ピン | 信号 |
|---|---|
| 1 | +5 V |
| 2 | A 相 |
| 3 | GND |
| 4 | B 相 |

- 512 ライン → 4逓倍で 2048 カウント/回転（0.176°/カウント）
- **インデックス（Z相）なし**。垂らした静止位置（α=π）でゼロ点を取る
- **電源は 5 V 必須。ESP32 は 3.3 V I/O なのでレベルシフタを必ず挟む**

### 実測済みの摩擦（2026/08/10）

**静止摩擦 τ_c = 0.147 N·m。** これが本プロジェクトの中心的な制約。
QUBE の素の寸法での必要制御トルクは約 0.014 N·m しかなく、**摩擦が約10倍**。

~~対策としてアーム延長（85→300 mm）と振子先端への錘追加（24→174 g）を行い、
必要トルクを約 0.154 N·m まで引き上げる。~~
**→ 2026/08/12 に判断の誤りを特定し撤回。素の寸法（QUBEモジュールそのまま、
アーム延長・錘なし）で成立する見込みが立ったため、延長ブラケット・錘・アルミ加工は
すべて不要になった。** 詳細は
[`倒立振子_ロードマップ.md`](倒立振子_ロードマップ.md) を参照。

**摩擦補償は必須。** 平衡点近傍では必要トルクが定義上ゼロに近づくため、
どれだけ設計をスケールアップしても必ず摩擦の不感帯に入る。摩擦を上回ることは原理的にできない。

### T_MAX を下げる（2026/08/12 に 2.0 N·m へ変更・実施済み）

`T_MAX=10 N·m` を 12bit で割ると 1 LSB = 4.9 mN·m で、制御トルクに対して粗すぎる。
**2026/08/12、RID 23 を 2.0 N·m に変更し、分解能を 0.98 mN·m に改善済み**
（`MotorScaling.TorqueMax` も同時に書き換え済み）。

**変更する場合は `MotorScaling.TorqueMax` を必ず同時に書き換える**
（指令とフィードバック両方の解釈が変わる）。変更後はトルク校正をやり直すこと。

---

## 重要な前提・注意

### `kp = 0` に関する README の記述を誤解しない

`README.md` に「**MITモードでは kp を 0 にしてはいけない**」とあるが、これは
**位置指令でモータを動かす場合の話**。`kp=0` かつ `t_ff=0` なら指令トルクが 0 なので
動かないのは当然の挙動であり、故障ではない。

**倒立振子では逆に `kp=0, kd=0` を意図的に使い、`t_ff` だけでトルクを与える。**
これが LQR 出力を直接流すための純粋トルクモードであり、本プロジェクトの前提。
既存の `MitCommand()` は既定値が `kp=20, kd=2` なので、**呼び出し時に明示的に 0 を渡すこと。**

### 制御ループのリアルタイム性

既存コードは「1回転させる」用途に作られており、**そのままでは定周期制御に使えない**。
新規に専用の固定周期ループを実装する際、以下に注意する。

- **`Thread.Sleep()` を周期生成に使わない。** Windows の既定タイマ分解能は約 15.6 ms で、
  `Thread.Sleep(5)` が 15 ms 眠りうる。`Stopwatch` + スピン待ち、または
  `timeBeginPeriod` の P/Invoke で分解能を上げる
- **制御ループ内でヒープ確保をしない。** 現状 `BuildMitFrame()` と
  `BuildSerialPacket()` は毎回 `byte[]` を確保している。定周期ループ用には
  バッファを事前確保して使い回す版を用意し、GC 由来のジッタを避ける
- GC は `SustainedLowLatency` を検討する
- **毎周期のタイムスタンプをログし、ジッタを数値で把握する**（Day 2 の作業）

### 安全

- **例外・Ctrl+C・正常終了のいずれでも必ず失能させる**（既存の `try/finally` +
  `IDisposable` + `CancelKeyPress` の作法を踏襲する）
- ホスト側ウォッチドッグ: 一定時間指令が途切れたらトルク 0
- 角度リミット、温度監視（`DriverTemperature` / `RotorTemperature`）
- エラーコードのパース（0=正常, 1=使能中, 8=過電圧, 9=低電圧, 0xA=過電流,
  0xB=MOS過温, 0xC=コイル過温, 0xD=通信喪失, 0xE=過負荷）
- トルクモードの暴走は速度モードと危険度が違う。**非常停止手段を手元に置く**

### 動かないときにまず疑う順序

1. **24V 電源が入っているか**（過去に最も時間を浪費した原因。モータの LED が
   赤＝失能 / 緑＝使能）
2. 送信 CAN ID のオフセット（MIT は `0x000+ID`、速度は `0x200+ID`）
3. `CTRL_MODE` がフラッシュに保存されているか
4. `MotorScaling` の定数とレジスタ実値の一致

**Enable / Disable はモータIDあてに送る。** `0x104` や `0x204` では受け付けられない。

**USB-CAN を開いた直後は 0.5 秒待つ**（`Motor` のコンストラクタで対処済み）。

---

## コーディング規約

- コメント・ドキュメントコメント・コンソール出力は**日本語**（既存コードに合わせる）
- XML ドキュメントコメント（`<summary>`）を public メンバに付ける
- 「なぜそうしたか」を書く。実機で判明した落とし穴は必ずコメントに残す
- 実機がなくても検証できるよう、`ICanBus` を挟んでテスト可能に保つ
- 単位を名前に含める（`speedRadPerSec`, `deltaRad`, `PositionRad`）

## ビルド・実行

```bash
cd モータ制御プログラム一式_20260806/csharp
dotnet build
dotnet test
dotnet run --project src/DemoOneTurn -- COM3
```

## 対話

- **やり取りは日本語で行う**
