# 作業報告：モータ制御プログラムの C# 移植と制御周期の実測

- 報告日: 2026/08/10
- 対象: Damiao DM-J4310-2EC V1.1（CAN ID = 4）
- 内容: 2026/08/06 の Python 実装を C# へ移植し、実機で動作確認・制御周期を実測

---

## 0. 要旨

- Python 版（`damiao_can.py` / `demo_1回転.py`）を C# へ全面移植し、**実機で 1 回転の動作を確認**した。
- **200Hz（5ms 周期）の制御ループは、標準偏差 3μs で安定して回せる**ことを実測で確認した。
- ただし途中で **1 周期あたり 15ms を食う実装上の落とし穴**があり、これを直して初めて 200Hz が出た（詳細は §5）。
- 律速は言語ではなく **USB-CAN アダプタの往復時間（約 1.8ms/周期）**。500Hz が実用上限、1kHz は届かない。

---

## 1. 制御周期【必須】

### 1.1 測定条件

- 使能コマンドは送らず、失能状態で速度指令フレーム（0x204、速度 0.0）を定周期で送信し、
  同じループ内でフィードバックを受信した。**モータは回していない。**
- 1 周期＝「送信 → 受信取り込み → 次周期まで待機」。各周期の実測間隔を全数記録した。
- `GCSettings.LatencyMode = SustainedLowLatency`、スレッド優先度 Highest を設定。
- 200Hz は 2000 サイクル（10 秒）、それ以外は各 3 秒相当。

### 1.2 待機方式の比較（目標 200Hz = 5.00ms、2000 サイクル）

| 待機方式 | 平均 | 中央 | 最小 | 最大 | 標準偏差 | p95 | p99 |
|---|---|---|---|---|---|---|---|
| **スピン待ち** | **5.000** | 5.000 | 4.956 | **5.044** | **0.003** | 5.001 | 5.011 |
| ハイブリッド | 5.000 | 5.000 | 1.359 | 9.761 | 0.140 | 5.005 | 5.118 |
| `Thread.Sleep` | 7.091 | 6.910 | 5.386 | 52.753 | 1.783 | 7.987 | 8.526 |

単位はすべて ms。

- **スピン待ち（`Stopwatch` を見ながら待つ）が圧倒的に安定。ジッタは最大でも ±44μs、σ=3μs。**
  ただし 1 コアを 100% 占有する（8 コアあるので実用上は問題なし）。
- ハイブリッド（残り 2ms までは `Sleep(1)`、最後だけスピン）は CPU を空けられるが、
  最大 9.76ms＝1 周期スキップ相当が発生する。
- **`Thread.Sleep` 単独は制御周期の生成に使えない。** 平均が 42% も伸び、最大 52.75ms に飛ぶ。

### 1.3 周波数を変えた場合（スピン待ち）

| 目標 | 平均 | 中央 | 最小 | 最大 | 標準偏差 | p99 | 判定 |
|---|---|---|---|---|---|---|---|
| 100Hz (10.00ms) | 10.000 | 10.000 | 9.843 | 10.156 | 0.022 | 10.125 | 余裕 |
| **200Hz (5.00ms)** | **5.000** | 5.000 | 4.982 | 5.018 | **0.002** | 5.010 | **余裕** |
| 500Hz (2.00ms) | 2.053 | 2.000 | 0.816 | 10.332 | 0.497 | 4.647 | 実用上限 |
| 1000Hz (1.00ms) | 1.662 | 1.180 | 0.031 | 20.488 | 0.947 | 4.810 | **届かない** |

- **200Hz までは目標周期をほぼ完全に維持できる。**
- 500Hz は平均 2.053ms（目標比 +2.7%）で回るが、p99 が 4.6ms まで伸びる。
- 1000Hz は平均 1.662ms で、そもそも目標に届かない。次項の I/O 時間が理由。

### 1.4 1 周期あたりの I/O 内訳

| 処理 | 平均 | 中央 | 最大 |
|---|---|---|---|
| 送信（CAN フレーム 1 本 = シリアル 13 バイト） | 0.599 | 0.598 | 2.154 |
| 受信（フィードバック 1 本の取り込み） | 1.160 | 1.166 | 1.484 |
| **合計** | **約 1.76** | | |

**I/O だけで約 1.8ms 必要**なので、5ms 周期（200Hz）に対しては 36% の占有で余裕があるが、
1ms 周期（1kHz）は物理的に不可能。これは **USB-CAN アダプタと CAN 往復の時間であり、
C# か Python かとは無関係**。

### 1.5 測定環境についての注意

**この測定は macOS 上のものです。** 研究室の実験を Windows で行う場合、次の理由で
再測定が必要です。

- Windows の既定タイマ分解能は約 15.6ms で、`Thread.Sleep` の挙動が macOS と大きく異なる
  （`timeBeginPeriod` の呼び出しが要る可能性がある）
- `System.IO.Ports` の実装が OS ごとに別物（Windows は Win32 API、Unix は termios）

ただし **スピン待ちは OS のタイマに依存しない**（`Stopwatch` = 高分解能カウンタを直接見る）ため、
Windows でも同等の精度が出る見込みです。

---

## 2. 実行環境【必須】

| 項目 | 内容 |
|---|---|
| 言語 | C# 14 |
| フレームワーク | .NET 10.0.10（SDK 10.0.302、LTS） |
| ターゲット | `net10.0`、`Nullable` / `ImplicitUsings` ともに有効 |
| OS | macOS 15.7.7（Apple Silicon, arm64, 8 論理コア） |
| 外部ライブラリ | **`System.IO.Ports` 10.0.10 のみ** |
| CAN 通信の実装 | **自前実装**（後述） |
| アダプタの扱い | **仮想 COM ポート（シリアルポート）として直接叩く。専用 DLL は使っていない** |
| ポート設定 | 2,000,000 bps / 8bit / パリティなし / ストップビット 1 / フロー制御なし |
| デバイス | CH340（VID 0x1a86 / PID 0x7523）→ `/dev/cu.usbserial-120` |

### CAN 通信をどう実装したか

Python 版は `python-can` の `seeedstudio` バックエンドが USB-CAN Analyzer を扱っていましたが、
**.NET には同等のライブラリが存在しません。** そのため、アダプタのシリアルプロトコルを
自前で実装しました（`SeeedCanBus.cs`）。

```
初期化   AA 55 12 [速度] [フレーム種別] [フィルタ4B] [マスク4B] [動作モード] 01 00×4 [CRC]
送受信   AA [0xC0|拡張<<5|RTR<<4|DLC] [ID 2B(標準) リトルエンディアン] [データ] 55
状態応答 AA 55 [18バイト]
```

CRC は先頭 2 バイトを除く総和の下位 8bit です。

**この移植が正しいことは、Python 版が実際に生成するバイト列を採取して期待値にした
自動テスト 87 件で検証しています**（`dotnet test`、実機不要）。
初期化フレーム・使能/失能・速度指令・モード読み書き・フラッシュ保存・MIT フレーム・
フィードバック解釈のすべてが Python 版とバイト単位で一致します。

---

## 3. 制御内容【必須】

| 項目 | 内容 |
|---|---|
| 制御モード | **速度モード（3 / VELOCITY）** |
| 指令値 | 0.5 rad/s（約 3 rpm） |
| 継続時間 | 12.6 秒（= 1.0 回転ぶん） |
| 指令の送出 | 約 5ms ごとに速度指令フレームを送り続ける |
| 停止処理 | 速度 0 を 30ms 間隔で 10 回送出 → 失能 |

モータのフラッシュには既に速度モードが保存されていたため、モード変更は行っていません。

MIT モード（`MitCommand` / `MitMove`）も実装済みですが、**今回は実機で試していません。**

---

## 4. 結果【必須】

### 4.1 回転角度

| | Python 版（Windows, 8/6） | C# 版（macOS, 8/10） |
|---|---|---|
| 指令 | 0.5 rad/s × 1 回転 | 同じ |
| 実測 | 359.5〜359.6 度 | **358.5 度** |
| 目標との誤差 | −0.4〜−0.5 度（−0.13%） | **−1.5 度（−0.42%）** |
| 速度のばらつき | ほぼ無し | ほぼ無し |

### 4.2 誤差についての考察

**速度制御の誤差ではありません。** 2 秒ごとの位置は

```
t=2.0s −1.72 → 4.0s −0.72 → 6.0s +0.29 → 8.0s +1.28 → 10.0s +2.29 → 12.0s +3.29 [rad]
```

と 2 秒あたり 1.00 rad 一定で、実速度は 0.5008 rad/s（指令比 +0.16%）でした。

誤差の正体は**計測窓**です。回転量は「最初に受信したフィードバック」から
「最後に受信したフィードバック」までの差分を積算しているため、
① 使能してから最初のフィードバックが届くまで ② 最後のフィードバック以降の減速停止中、
の 2 区間が計上されません。位置データから逆算すると、今回欠けたのは約 70ms 相当でした
（Python 版は約 18ms 相当）。**Python 版と同じアルゴリズムであり、移植の誤りではありません。**

なお、この計測方法は両版とも数十 ms 分を取りこぼす作りになっています。
正確に測るなら、停止シーケンス中もフィードバックを積算する必要があります。

### 4.3 フィードバックの取得

**すべて問題なく取得できました。**

| 項目 | 取得値の例 |
|---|---|
| 位置 | −2.70 rad →（12 秒後）+3.29 rad、連続取得 |
| 速度 | 取得可（12bit、±30 rad/s レンジ） |
| トルク | 取得可（12bit、±10 N·m レンジ） |
| エラーコード | 0（正常）を維持 |
| ドライバ温度 / ロータ温度 | 28℃ / 27℃、走行中の上昇なし |

定周期ループでの受信取りこぼしも確認しました。**200Hz で 2000 サイクル送信して
応答 2000 件**（取りこぼし 0）。500Hz で 1500 件、1000Hz で 3000 件と、
どの周波数でも指令 1 本に対しフィードバック 1 本が確実に返っています。

このほか、バススキャン（CAN ID 1〜8 を順に叩いて応答を探す）で ID=4 のみを正しく検出、
パラメータ読み出し（0x7FF あて）で制御モード 3 を読み出せることも確認済みです。

---

## 5. つまずいた点と解決方法【あると良い】

### 5.1 `SerialPort.ReadByte()` が 1 回 1.16ms かかる（最大の落とし穴）

**症状**: 目標 200Hz（5ms）で回そうとしても、待ち方を何に変えても
**実周期が 16ms（≒62Hz）から下がらない**。1000Hz を狙っても 15.9ms。

**切り分け**: 素の `SerialPort` で「書き込み＋スピン待ち」だけのループは 5.000ms で
回ることを確認。差分は受信処理だけだった。そこで受信の内訳を計測したところ、

| 読み方 | 1 回あたり |
|---|---|
| `ReadByte()` を 1 バイトずつ | **1.156ms** |
| `Read(buffer, 0, n)` でまとめ読み | 0.290ms（13 バイト分） |

**原因**: 移植当初、受信パーサを Python 版と同じ「1 バイトずつ読んで解釈する」構造で
書いていた。CAN フレーム 1 本はシリアル 13 バイトなので、
**13 × 1.16ms ≒ 15ms** がまるまる 1 周期に乗っていた。
Python（pyserial）では 1 バイト読みが安いため顕在化しない問題で、
.NET の `SerialPort` に固有の落とし穴。

**解決**: 受信を「`BytesToRead` 分を一括で読んで内部バッファに貯め、
パーサはそのバッファから取る」方式に変更。

```csharp
// 受信は必ずまとめ読みしてバッファに貯め、パースはそこから行う
private int ReadByte(long deadline)
{
    if (_receiveHead < _receiveTail) return _receiveBuffer[_receiveHead++];

    int available = _port.BytesToRead;
    if (available > 0)
    {
        _receiveTail = _port.Read(_receiveBuffer, 0, Math.Min(available, _receiveBuffer.Length));
        _receiveHead = 0;
        ...
    }
}
```

**効果**: 受信 15.08ms → **1.16ms**、実周期 16.3ms → **5.000ms**。

### 5.2 `python-can` に相当するライブラリが .NET に無い

アダプタのシリアルプロトコルを自前実装した（§2 参照）。
正しさは Python 版から採取したバイト列との一致テスト 87 件で担保している。

### 5.3 Ctrl+C でプロセスが即死し、失能処理が走らない

C# は既定だと Ctrl+C で `finally` を通らずに終了する。Python 版と同じ安全性を保つため、
`Console.CancelKeyPress` を捕まえて減速停止 → 失能に回すようにした。

### 5.4 `Spin(speed: 0)` が無限ループになる

Python では所要時間の計算がゼロ除算で例外になるが、C# の `double` は無限大になり
**回り続けてしまう**。明示的に例外を投げるようにした。

### 5.5 macOS の 2Mbps は問題なかった

macOS の termios には Linux と違い `B2000000` の標準定数が無いため懸念していたが、
`SerialPort` で 2,000,000 bps を指定して正常に通信できた。

---

## 6. Python と比べての所感【あると良い】

### 書きやすさ

**Python の方が短く速く書ける。** 今回の移植でコード量はおよそ 2.5 倍になった
（Python 約 310 行 → C# 約 800 行、テストを除く）。型宣言、プロジェクト構成、
`csproj` の管理といった Python には無い手間がかかる。

一方で **C# は「壊れにくさ」で優る**。

- `null` になりうる箇所がコンパイル時に分かる（`Nullable` 有効）
- `ControlMode.Velocity` のような列挙型で、マジックナンバー 3 の取り違えが起きない
- IDE の補完とリファクタリングが確実に効く

倒立振子のように**状態量が増えて式が複雑になる**コードでは、この差は効いてくると思う。

### 速さ

**実行速度は今回の用途では差が出ない。** 律速は USB-CAN アダプタの往復（約 1.8ms/周期）で、
言語処理系の速度ではない。C# の計算が速くても、アダプタが待ってくれない。

ただし**リアルタイム性の制御手段は C# の方が揃っている**。

| | C# | Python |
|---|---|---|
| GC の制御 | `SustainedLowLatency` などで明示指定できる | 実質不可（`gc.disable()` 程度） |
| スレッド優先度 | 指定できる | OS 依存で扱いにくい |
| 高分解能タイマ | `Stopwatch`（今回 1ns 分解能） | `time.perf_counter()` で同等 |
| ヒープ確保の回避 | `Span`、事前確保、構造体で可能 | 難しい |

今回 200Hz × 2000 サイクルの測定で **GC 発生 0 回**を達成できたのは、
事前確保とレイテンシモード指定を明示的に行えたため。

### テストのしやすさ

**ここが一番大きな差だった。** CAN バスをインターフェースにして偽バスに差し替えることで、
**実機が無い状態で 87 件の検証を自動化**できた。移植中に「Python 版と同じバイト列が出ているか」を
何度でも確認できたのは C#（というより静的型付け＋テスト基盤）の恩恵が大きい。

---

## 7. ソースコードの要点【あると良い】

構成（`モータ制御プログラム一式_20260806/csharp/`）:

| ファイル | 役割 |
|---|---|
| `src/DamiaoCan/Motor.cs` | 使能/失能・状態取得・モード読み書き・速度制御・MIT 制御 |
| `src/DamiaoCan/SeeedCanBus.cs` | USB-CAN アダプタのシリアルプロトコル（自前実装） |
| `src/DamiaoCan/ICanBus.cs` | CAN バスの抽象。テストや別アダプタ用の差し替え口 |
| `src/DamiaoCan/MotorFeedback.cs` | 8 バイトフィードバックの解釈 |
| `src/DamiaoCan/MotorScaling.cs` | MIT フレームの値域と実数⇔整数の換算 |
| `src/DemoOneTurn/Program.cs` | 1 回転デモ |
| `tests/DamiaoCan.Tests/` | 実機不要の自動テスト 87 件 |

### 定周期ループの骨格（今回 200Hz を達成した形）

```csharp
long periodTicks = Stopwatch.Frequency / 200;        // 200Hz
long deadline = Stopwatch.GetTimestamp() + periodTicks;

for (int i = 0; i < cycles; i++)
{
    bus.Send(frame);                                  // 指令を送る
    if (bus.Receive(TimeSpan.Zero) is { } feedback)   // フィードバックを取り込む
        Update(feedback);

    // Thread.Sleep は使わない。Stopwatch を見ながらスピン待ちする
    while (Stopwatch.GetTimestamp() < deadline) Thread.SpinWait(20);

    deadline += periodTicks;
    if (Stopwatch.GetTimestamp() > deadline)          // 大きく遅れたら詰めずに刻み直す
        deadline = Stopwatch.GetTimestamp() + periodTicks;
}
```

要点は 3 つ。

1. **`Thread.Sleep` を周期生成に使わない**（§1.2 の通り最大 52ms に飛ぶ）
2. **ループ内でヒープを確保しない**（フレームとバッファは事前確保して使い回す）
3. **遅れたときに取り戻そうとして詰めない**（連続で短周期になり不安定化するため）

### 安全対策

- 例外・Ctrl+C・正常終了のいずれでも**必ず失能**する（`try/finally` ＋ `IDisposable`）
- `Spin` / `MitMove` は `CancellationToken` で中断でき、中断時も減速停止 → 失能を通る
- 上記の挙動はテストで検証済み（例外発生時に失能フレームが最後に送られることを確認）

---

## 8. 相談事項への回答：倒立制御を C# と Python のどちらで実装するか

### 判断材料「200Hz を安定して回せるか」について

**C# は回せます。実測で平均 5.000ms、標準偏差 3μs、最大偏差 ±44μs。**
200Hz に対しては十分すぎる精度で、余力もあります（500Hz まで実用範囲）。

ただし、**この判断材料だけでは C# を選ぶ根拠として弱い**と考えます。理由は 2 つあります。

**1. 律速はアダプタであって言語ではない。**
1 周期の I/O に約 1.8ms かかっており、これは USB-CAN アダプタと CAN 往復の物理時間です。
Python でも同じ 1.8ms がかかり、残り 3.2ms を Python で使い切ることは考えにくい。
**Python でも 200Hz は出る可能性が高い**と思います（未測定です）。

**2. C# にも 200Hz を殺す落とし穴があった。**
§5.1 の通り、素直に書いたら 62Hz しか出ませんでした。
「言語が速いから大丈夫」ではなく、**どちらの言語でも実測しないと分からない**というのが実感です。

### メンバーBの意見

**C# を推します。ただし理由は速度ではありません。**

1. **実機なしで検証できる基盤がすでにできている。**
   87 件の自動テストがあり、CAN バスを差し替えられる構造になっています。
   倒立制御では状態推定・LQR ゲイン・スイングアップと検証したいロジックが増えます。
   モータを回さずにロジックを詰められる価値は、開発期間を考えると大きいと思います。

2. **状態量が増えたときに壊れにくい。**
   倒立振子はアーム角・振子角・それぞれの角速度と、扱う値が増えます。
   単位や符号の取り違えが起きやすい領域で、静的型付けと列挙型が効きます。

3. **研究室の方針が C# であり、資産が引き継がれる。**
   今回作った通信層はそのまま流用できます。

### 一方で、Python を選ぶ合理性もあります

- **試行錯誤の速さ**。ゲイン調整のように「値を変えて即試す」作業は Python が有利です。
- **数値計算ライブラリ**。状態推定や LQR ゲインの設計で NumPy / SciPy / control を使うなら、
  Python の方が圧倒的に楽です。C# には同等の成熟したライブラリがありません。
- **8/6 の動作実績がある**（ただし C# も 8/10 に実機で 1 回転を確認済み）。

### 提案：役割を分けるのが現実的では

- **ゲイン設計・シミュレーション・モデル同定は Python**（NumPy / SciPy / control を使う）
- **実機の制御ループは C#**（求めた最適ゲインを定数として渡す）

制御ゲインは実行時に計算するものではないので、この分け方なら両方の利点を取れます。

### 決める前にやるべきこと

**Python 側でも同じ 200Hz のジッタ測定を行うことを提案します。** 今回 C# で使った測定手順は
そのまま Python に移せます（送信 → 受信 → `perf_counter` でスピン待ち、全周期の間隔を記録）。
30 分程度で終わり、**憶測でなく実測で比較できます。**
C# が 5.000ms / σ3μs という数字を出しているので、比較の基準は明確です。

---

## 付録：未実施・未確認の事項

正確を期すため、**今回確認できていないこと**を明記します。

- **Windows での測定**（今回はすべて macOS）。研究室が Windows なら再測定が必要
- **MIT モードの実機動作**。実装とテストは済んでいるが、実機では試していない
- **モータを実際に回しながらの 200Hz 測定**。ジッタ測定は失能状態（安全のため）で実施
- **長時間運転での温度上昇・安定性**。今回の連続運転は最長 12.6 秒
- **Python 版の 200Hz ジッタ**。比較対象として未測定
