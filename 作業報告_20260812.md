# 作業報告 2026/08/12（水）

**件名**: DM-J4310-2EC を用いた回転型倒立振子（Furuta pendulum）

ロードマップ上の **Day 2 を完了し、Day 3 を前倒しで完了**した。
あわせて機構設計（アダプタ・校正治具）と振子パラメータの同定まで進んだ。

---

## 1. 実機作業（モータ）

| 項目 | 結果 |
|---|---|
| MIT モードへ移行 | 完了。フラッシュ保存し、電源再投入後も `CTRL_MODE=1` を維持 |
| レジスタ照合 | `P_MAX=12.5` / `V_MAX=30` / 減速比 `10:1` を CAN 経由で直読し、コードの定数と一致を確認 |
| **T_MAX 変更** | **10 → 2.0 N·m**。12bit 分解能が 4.9 → **0.98 mN·m** と5倍細かくなった |
| 逆駆動テスト | 合格。失能時と使能時（トルク0）で感触に差が無く、寄生トルクなし |
| 静止摩擦 | **0.147 N·m**（T_MAX 変更後に再測して 0.137、7%以内で整合） |
| 動摩擦 | **0.126 N·m**（正転 0.109 / 逆転 0.143、30% 非対称） |
| 粘性摩擦 | **0.027 N·m·s/rad** |
| 制御周期 | **300 Hz に決定**（下表） |

### 制御周期の実測（素のモータ、各12秒）

| 目標 | 実測平均 | 標準偏差 | 95%点 | 最大 | 取りこぼし |
|---|---|---|---|---|---|
| 200 Hz | 5.000 ms | 0.820 | 5.92 | 21.7 | 0% |
| **300 Hz ←採用** | **3.333 ms** | **0.563** | **3.75** | 21.8 | 0% |
| 400 Hz | 2.500 ms | 0.851 | 3.61 | 33.1 | 0% |
| 500 Hz | 2.294 ms ✗ | 1.158 | 3.52 | 38.3 | 0% |

500 Hz は追いつけず実質 436 Hz。**USB-CAN の往復遅延が約 2.3 ms** で、これが上限を決めている。
振子の時定数（約 90 ms）に対して常用遅延 5 ms は 6% であり、制御上問題ない。

### T_MAX 変更の検証方法

レジスタを書き換えただけでは、指令とフィードバックの解釈が本当に整合しているか分からない。
そこで**既に測ってある静止摩擦を測り直し**、同じ物理量が同じ値で出るかを確認した。
不整合なら 5 倍または 1/5 倍にずれるはずのところ、0.147 → 0.137 と 7% 以内で一致したため整合と判断。
副次的に、測定のばらつきが 0.133〜0.147 → 0.122〜0.129 と狭まり、分解能向上が実測精度にも表れた。

**変更はモータ側 → コード側の順で行った。** 逆順だと「コードは小さい T_MAX、モータは大きい T_MAX」
となり、同じ指令値が数倍のトルクとして解釈されて危険なため。

---

## 2. 制御ソフト（C# / .NET 10）

### ライブラリに追加したもの

| API | 内容 |
|---|---|
| `Motor.TorqueCommand(torqueNm)` | 純粋トルク指令。`kp=kd=0` を型で保証 |
| `Motor.ReadFeedback(timeout)` | 定周期ループ用のフィードバック読み出し |
| `Motor.ReadParameter / ReadParameterSingle / ReadParameterUInt32` | レジスタ読み出しの汎用化 |
| `Motor.WriteParameterSingle / SaveToFlash` | レジスタ書き込みとフラッシュ保存 |
| `Motor.VerifyScaling()` | **トルクを出す前にレジスタと定数の一致を検証する安全網** |
| `RegisterId` | パラメータ番号の定義（7〜23） |

### 設計上の修正

`Motor.BuildMitFrame` がグローバル定数（`MotorScaling`）に暗黙依存していたため、
T_MAX を変更すると「Python 版と同じバイト列を出す」という回帰テストの前提ごと壊れた。
**値域を引数で渡せるようにし、詰め方の検証と値域設定を分離**した。

### 作成したツール

| ツール | 用途 |
|---|---|
| `SetControlMode` | モード確認・変更、レジスタ照合、T_MAX 変更 |
| `BackdriveTest` | 逆駆動テスト、制御周期のジッタ計測 |
| `FrictionTest` | 静止摩擦 / 動摩擦・粘性の測定（3モード） |
| `TorqueCalibration` | トルクスケール校正、重力補償デモ（未実行） |

**単体テスト 88 件すべて合格。**

---

## 3. モデリングと制御設計

### 実施内容

- **SymPy による Furuta の運動方程式導出**（手計算では Coriolis 項の符号を誤るため）
- 平衡点での線形化 `A`, `B`。可制御性ランク 4/4
- LQR（Bryson の法則）と定常カルマンフィルタの設計
- **実測の遅延・ジッタ・量子化・非対称クーロン摩擦をすべて入れた非線形閉ループ検証**
- 摩擦モデルを `tanh` から **Karnopp（真の固着あり）** に差し替えた再検証
- 摩擦補償の効き・アーム慣性についての感度解析

### 振子パラメータの同定（実測から）

| パラメータ | 値 | 出所 |
|---|---|---|
| 振子全長 | 128.70 mm | 実測 |
| ピボットより上 | 7.0 mm | 実測 |
| **l_p（ピボット→重心）** | **57.35 mm** | 上記から算出 |
| **J_pivot** | **1.123×10⁻⁴ kg·m²** | 自由振動 T=0.5730 s から |
| **J_p（重心まわり）** | **3.33×10⁻⁵ kg·m²** | 同上 |
| **b_p（振子軸の粘性）** | **2.45×10⁻⁵ N·m·s/rad** | 対数減衰 δ=0.0624/往復 |
| 不安定極 | **11.17 rad/s（時定数 90 ms）** | モデルから |

**幾何形状からの予測（J_pivot = 1.121×10⁻⁴）と周期からの逆算が 0.17% で一致。**
「一様な棒が端から 7 mm で吊られている」という仮定が正しいことの裏付けとなった。

なお、測定された「20往復で 5.73 秒」は物理的にありえない（質量が全部重心に集中していても
J_pivot は 7.9×10⁻⁵ 以上あり、T=0.285 秒には 2.8×10⁻⁵ が必要）。
**10往復（片道20回）と解釈**すると幾何予測と 0.2% で一致するため、そちらを採った。

### 検証結果（Karnopp 摩擦、実測パラメータ）

許容トルク（Bryson の重み）を掃引した結果、**0.005〜0.01 N·m 付近に明確なスイートスポット**があり、
そこでリミットサイクルは **0.15〜0.5°**。
L_r を 85〜120 mm、J_r を 0.3〜3.0 g·m² に振っても成立する。

⚠️ **ゲイン感度が高い。** 許容トルクを 0.01 → 0.02 に変えるだけで 0.24° → 5.37° と 20 倍以上悪化する
（非単調）。実機ではシミュレーションの最適点が最適とは限らないため、
Day 6 では対数的に振って探す前提でいること。

---

## 4. 機構設計

### QUBE モジュール取付アダプタ（4部品）

| 部品 | 厚み | 役割 |
|---|---|---|
| スペーサリング | 4 mm | 回転フランジ（Ø34.95）と固定ハウジング（Ø57）の干渉回避 |
| ベース板 | 6 mm | モータ出力フランジへ固定。M4 締結穴、Ø4 ピン穴 |
| クレードル板 | 20 mm | 外開き U 字溝。**溝の壁でトルクを受ける** |
| キータイル | 3.5 mm | モジュールの窪み（23×23）を埋め、形状拘束と磁石保持を兼ねる |

**トルク伝達は磁石ではなく形状とネジで受ける設計とした。** PLA はボルト予圧でクリープするため、
摩擦だけに頼る締結は経時的に緩み、「制御中に時々ズレる」という発見しにくい壊れ方をする。

- ヨー（制御トルクの方向）: 溝の壁 ＋ Ø4 位置決めピン
- 締結: モジュール側面の M4 ネジ穴へ下から直結
- 磁石: 保持のみ

### トルク校正用アーム

左右対称形状（全長 170 mm、錘穴は r = 75 mm）。
**対称にすることでアーム自身の重力トルクが完全に相殺され、先端に付けた錘の m·g·r だけが残る。**
校正の基準そのものが正確になる。A1 mini（180 mm）に収まる寸法に設計変更済み。

### 自動検証

STL 生成スクリプトには寸法の整合性チェックを実装した。
本日の作業中、**穴同士の干渉・造形サイズ超過・スペーサとハブ径の不整合・
M4 穴と M3 スロットの干渉など、計 5 件を自動検出**して手戻りを防いだ。
生成物はすべて多様体チェックを通過し、三角形分割の総面積が解析値と誤差 0.0000 mm² で一致。

---

## 5. 判断を変更した点（重要）

本日の検証により、以前の判断のうち **4 件が誤りと判明**した。記録として残す。

### ① 「摩擦が必要トルクの10倍あるのでアーム延長 300 mm が必須」→ 誤り

必要な定常トルク（0.014 N·m）と摩擦（0.126 N·m）を比べて「10倍だから無理」と判断していたが、
**モータは 2.0 N·m 出せる**。摩擦は越えられない壁ではなく打ち消すべき外乱であり、
制御器は必要なだけトルクを積み増して振り切ればよい。

比べるべきは「必要な定常トルク 対 摩擦」ではなく「**使えるトルク 対 摩擦**」だった。

素の寸法で転倒した最初のシミュレーションは、**改造後のプラント用に調整したゲインを
そのまま使った不公平な比較**であり、対照実験（摩擦ゼロ）でも 0.04 秒で転倒した。
素の寸法はトルク応答が 10 倍敏感（B[3] = −2602 rad/s²/N·m）なため、
同じ重みでは閉ループ極が 530 rad/s まで上がり離散化で不安定化していた。

**→ アーム延長ブラケット（215 mm）、錘（150 g）、アルミ加工は不要になった。**

### ② 「摩擦補償は必須」→ 必須ではない

Karnopp モデル（真の固着あり）での検証では、**補償なしでも成立**した（1.69°）。
さらに、**固着ありの方が tanh 近似よりリミットサイクルが小さい**（0.41° 対 0.96°）。
静止摩擦は関節を実際に止めるため、「その場に留める」問題では味方に回る。

スティクションが有害なのは追従制御や精密位置決めであって、倒立振子では逆に働く。

ただし前滑り変位や摩擦の履歴は Karnopp でも表現できないため、補償は入れておく方針は維持する。

### ③ 「スリップリングが必要」→ 不要

エンコーダのケーブルは**エンコーダ本体（ブロック側）から出ており、回転するのは銀の丸棒**。
振子が何回転してもケーブルは回らない。アームもスイングアップ中に往復するだけで連続回転しない。
**サービスループ（たるみ）で足りる。**

### ④ 「取り付け平面を下に伏せて板に載せる」→ 不可能

取り付け平面は**円筒に 3.5 mm 掘り込まれた窪み**であり、円筒面の方が外側に出ている。
平らな板に置いても平面は接触しない。
M4 ネジ穴が円筒面にあるという指摘で判明。**円筒面で受け、M4 で締結する構成に変更した。**

---

## 6. 未解決事項

| 項目 | 内容 |
|---|---|
| Ø4 ピン穴の角度 | M3 穴と一直線か中間か。**目視確認のみで判明する**（半径差 1.35 mm のため同一角度だと穴が重なる） |
| 溝の嵌合 | `gauge` を印刷して現物合わせ |
| L_r（実効アーム長） | 組み立て後に実測。85〜120 mm の範囲では結果が変わらないことは確認済み |
| モジュールの質量 | `J_r` の推定精度が上がる |
| トルクスケール校正 | `TorqueCalibration` は作成済み、実行は腕と錘（340〜510 g）待ち |
| `FurutaGains.cs` | 実測パラメータでの再生成が必要 |

---

## 7. 成果物

```
探求/
├── CLAUDE.md                          プロジェクト方針（C# で設計する等）
├── 倒立振子_ロードマップ.md            全9回の計画と実測値
├── 作業報告_20260812.md               本書
├── model/
│   ├── furuta_model.py                SymPy による導出＋線形化
│   ├── design_and_simulate.py         LQR・カルマン設計＋閉ループ検証
│   ├── friction_study.py              Karnopp 摩擦での再検証
│   ├── pendulum_identification.py     自由振動からのパラメータ同定
│   └── FurutaGains.cs                 C# へ埋め込む定数
├── 機構/
│   ├── ハードウェア設計_引き継ぎ書.md   共同作業者向け
│   ├── generate_calibration_arm.py    校正アームの STL 生成
│   ├── generate_qube_adapter.py       アダプタ4部品の STL 生成
│   └── *.stl                          生成物
└── モータ制御プログラム一式_20260806/csharp/
    ├── src/DamiaoCan/                 ライブラリ（API 追加）
    ├── src/SetControlMode/            モード・レジスタ操作
    ├── src/BackdriveTest/             逆駆動テスト・ジッタ計測
    ├── src/FrictionTest/              摩擦測定
    ├── src/TorqueCalibration/         トルク校正・重力補償
    └── tests/                         88 件合格
```

---
---

# 【後半】機構設計の確定と、実機での共振測定

前半の報告後にさらに進んだぶんを追記する。

## 8. ★本日最大の成果★ 実機で振子の固有振動数を測定した

**振子エンコーダが未着荷（ジャック待ち）のまま、モータ自身のフィードバックだけで
振子の固有振動数を測定することに成功した。**

### 方法

アームを MIT モードの位置制御で正弦波駆動し、周波数を掃引する。
振子が共振すると反力がアームに返り、位置制御の負荷が変わる。
つまり**振子側にセンサが無くてもモータ側の数値に痕跡が出る**。

### 結果（±8°、kp=20、1.30〜2.30 Hz）

| 周波数 [Hz] | 追従誤差RMS [deg] | トルクRMS [N·m] |
|---|---|---|
| 1.30 | 0.730 | 0.1319 |
| 1.50 | 0.790 | 0.1354 |
| **1.60** | 0.841 | **0.1437 ← ピーク** |
| 1.70 | 0.855 | 0.1409 |
| **1.80** | 0.874 | **0.1395 ← ディップ** |
| 1.90 | 0.914 | 0.1416 |
| 2.10 | 0.961 | 0.1448 |
| 2.30 | 1.024 | 0.1498 |

慣性負荷による単調増加（ω² に比例）の上に、**ピーク（1.60）とディップ（1.80）の対**が乗っている。
これは基礎加振された振子の典型的な挙動で、共振点より下では振子が同相に動いて実効慣性を増やし、
上では逆相になって減らす。**遷移点＝固有振動数は 1.70 Hz。**

### 意義

**自由振動から予測した 1.745 Hz と 3% 以内で一致した。** これにより3点が同時に裏付けられた。

1. **モデルが正しい** — 机上の予測が実機で再現された
2. **アダプタがトルクを伝えている** — 滑っていれば振子と結合せず、この構造は現れない
3. **組み立てが健全** — 結束バンドでの仮固定でもこの加振レベルでは十分

ロードマップ上は Day 5 のパラメータ同定に相当する内容を、実機で前倒しできたことになる。

### 併せて確認したこと

| テスト | 結果 |
|---|---|
| 弱加振（±3°、1.0 Hz、6秒） | 追従誤差 1.33 → 1.28°。**滑り・ドリフトなし** |
| 中加振（±5°、1.75 Hz、8秒） | 誤差 0.75° で安定 |
| 共振加振（±10°、1.68 Hz、16秒） | 誤差 0.98° で定常。発散も劣化もなし |

追従誤差が摩擦の不感帯で決まることも確認できた
（kp=4 では 1.3°、kp=15 では 0.75°、kp=20 では 0.98°／振幅増）。
摩擦 0.126 N·m を kp で押すのに必要な偏差 τ_c/kp とよく一致する。

### 限界

振子（24 g）の反力は振幅 0.5 rad でも **8 mN·m** 程度で、摩擦 126 mN·m の 7% しかない。
そのためトルク RMS の変化は 3% 程度にとどまり、**信号は弱い**。
また固定周波数では振幅増加に伴う固有振動数の低下を追えないため、真上までは上がらない。
**本来のスイングアップには振子角の計測が必須**であり、これはエンコーダ着荷後の課題。

---

## 9. モータ図面の確定

公式図面とメンバーA氏の実測データにより、出力側の寸法がすべて確定した。

| 項目 | 値 |
|---|---|
| 回転する出力フランジの外径 | **Ø34.95**（実測） |
| ボルト円（PCD） | **Ø27**、6×M3、深さ 5mm |
| 位置決めピン穴（大） | 3×Ø4（+0.02/+0.04）、PCD **Ø24.30** |
| 位置決めピン穴（小） | 2×Ø3（+0.02/+0.04）、深さ 5mm、基準から 15° |
| フランジの出っ張り | **1 mm**（実測。図面の 45/46 の差と一致） |
| 固定側ハウジング | Ø50 に 6×M3（**使わない**） |

図面の「Ø24.30」は位置決めピンの配置円であることが判明した。

---

## 10. アダプタ設計の確定（v8）

### 最終構成（4部品）

| 部品 | 寸法 | 体積 | 役割 |
|---|---|---|---|
| スペーサリング | Ø34 × 4 | 2.3 cm³ | 固定ハウジングとの干渉回避（隙間 5mm） |
| **ベース板** | Ø86 × 8 ＋ パッド 3.3 ＋ ペグ 5 | — | モータへ固定。**パッドとペグを一体成形** |
| 側面レール ×2 | 30 × 12 × 20 | 6.8 cm³ ×2 | 円筒を横から挟んでヨーを止める |
| 校正アーム＋錘 | 全長 170 / Ø60 | 28 / 81 cm³ | トルクスケール校正用 |

### トルクの経路

```
モータ → ベース板 → 一体パッド（23×23）→ 窪みの壁 → モジュール
                  ＋ M4 締結（R37.6、下から）
                  ＋ 側面レール（横方向）
```

**磁石にはトルクを持たせていない。** PLA はボルト予圧でクリープするため、
摩擦だけに頼る締結は経時的に緩み、「制御中に時々ズレる」という
発見しにくい壊れ方をする。形状とネジで受ける設計とした。

### 主要寸法

- ピボット高さ **26.0 mm**（スペーサ4 ＋ ベース8 ＋ 円筒半径14）
- 振子先端はモータ背面より **49.7 mm 下** → **固定治具は机から 70 mm 以上持ち上げる**
- 板厚 8mm は M4 ネジ（全長19.4／頭4.1）に合わせた値。ねじ込み 7.3mm

---

## 11. 判断を変更した点（後半ぶん）

### ⑤ 「取り付け平面を下に伏せて板に載せる」→ 不可能

取り付け平面は**円筒に 3.5 mm 掘り込まれた窪み**であり、円筒面の方が外側に出ている。
平らな板に置いても平面は接触しない。
**M4 ネジ穴が円筒面にあるという指摘で判明した。**
→ 円筒面で受け、M4 で締結する構成に変更。

### ⑥ 「キータイルを別部品にする」→ ベース板と一体成形へ

別部品にすると
- タイルの固定ネジがベース板の他の穴と干渉する
- **そもそもタイルをベース板に固定しないとトルクの経路にならない**（タイルが浮いていると
  「モジュール ⇄ タイル」は結合しても「タイル ⇄ ベース板」が結合しない）

の二つの問題が出た。一体成形にして両方解消。
そのために**多段押し出しのメッシャを新規実装**した。

### ⑦ 「クレードル円板」→ 側面レール2個へ

モジュール後端の形状が不定であり、そこに触れる必要もない（横方向だけ支えれば足りる）。
**印刷量が 93 cm³ → 13.6 cm³ と 1/7 になった。**

### ⑧ 「校正には 340〜510 g の金属錘が必要」→ PLA 製 100 g で足りる

「摩擦の2〜3倍必要」の根拠は、摩擦の正逆非対称（0.109 対 0.143）が
上げ下げの平均に −0.017 N·m の系統誤差として残ることだった。
**この非対称量は実測済みなので解析側で差し引ける。**

循環論法にはならない。トルクスケールが k 倍ずれていれば摩擦の実測値も
同じく k 倍ずれるため、補正後の比はきれいに 1/k になる。

→ 必要質量が 100 g まで下がり、**3Dプリント製の錘（Ø60×28.9、81 cm³）で校正可能**になった。

---

## 12. 実装した機能（後半ぶん）

| 対象 | 内容 |
|---|---|
| `generate_qube_adapter.py` | **多段押し出しメッシャ**（入れ子の段を積む立体） |
| 同上 | 寸法検証を拡充。本日 M4 穴の板外飛び出しなど計 6 件を自動検出 |
| `generate_calibration_arm.py` | PLA 製錘の生成（目標質量から寸法を逆算） |
| `TorqueCalibration` | 摩擦の非対称による系統誤差の補正（`--friction-pos/neg`） |
| **`OpenLoopSwing`（新規）** | 振子エンコーダ不要の共振加振・周波数掃引 |

単体テストは **88 件すべて合格**を維持。

---

## 13. 未解決事項（更新）

| 項目 | 状況 |
|---|---|
| 振子エンコーダの配線 | **ジャック待ち**。これが本番の律速 |
| Ø4 ピン穴の角度 | 目視確認のみで判明する（M3 穴と一直線か中間か） |
| 新ベース板の印刷 | 現状は結束バンドでの仮固定。加振レベルでは問題ないが本番前に要交換 |
| 溝・ペグの嵌合 | ゲージとキータイルで現物合わせ |
| トルクスケール校正 | ツール・治具とも完成。錘の印刷待ち |
| `FurutaGains.cs` | 実測パラメータでの再生成が必要 |
| 固定治具 | 机から 70 mm 以上持ち上げる必要がある。未設計 |

---

## 14. 所感

本日は「実機で確かめられることは全部確かめる」方針で進めた結果、
**机上の判断のうち 8 件が誤りと判明**した。いずれも実測または対照実験で覆っている。

特に ①（摩擦10倍だからアーム延長必須）と ⑤（平面を下に伏せる）は、
そのまま進めていれば**アルミ加工と 215mm ブラケットの製作、
および組めない部品の印刷**という形で顕在化していた。

一方、**モデルの予測精度は高い**。振子パラメータの同定では幾何予測と周期からの逆算が
0.17% で一致し、固有振動数の実機測定でも予測と 3% で一致した。
モデルは信頼してよく、疑うべきは前提条件の方だった、というのが本日の教訓。
