# 回転型倒立振子（Furuta）製作ロードマップ

- **アクチュエータ**: DM-J4310-2EC V1.1（減速比 10:1、MIT モードによるトルク制御）
- **振子部**: Quanser **QUBE-Servo 2 回転振子モジュール一式**（磁石台座付き、入手済み）
- **期間**: 2026/08/10（月）〜 2026/08/27（木）、月・水・木の週3回 × 3週 = 全9回
- **目標**: スイングアップ付き回転型倒立振子の完成

**Day 1（8/10）完了済み。** MIT モード移行、レジスタ照合、純粋トルク指令 API、逆駆動テスト、静止摩擦の実測。

---

## 全体方針

平衡制御は **LQR による状態フィードバック**。極配置は重みの物理的意味が不明瞭で試行錯誤になり、
PID カスケードは4状態を2ループに分割するためチューニングが困難、MPC は初号機には過剰。

制御ループは **PC 側（C#）に置く**。速度モードで既に動作している資産を活かす。
ESP32 は E8P エンコーダを読んで USB シリアルに垂れ流すだけの役割に限定する。

---

## 中心的な制約：摩擦が必要トルクの10倍ある

Day 1 に実測した静止摩擦は **τ_c = 0.147 N·m**（定格 3 N·m の約5%。10:1 遊星ギヤとして正常な値）。

一方、QUBE-Servo 2 の素の寸法（アーム 85 mm / 95 g、振子 129 mm / 24 g）で
平衡制御に必要なアームトルクは **約 0.014 N·m** しかない。**約10倍の差。**

QUBE 本体のモータは最大 0.02〜0.03 N·m 級であり、**我々の摩擦だけで QUBE のモータの
全トルクの5倍以上ある**。QUBE はダイレクトドライブ前提の設計だからこれで成立している。

摩擦補償で埋められるのは良くて7〜9割なので、**機構側で必要トルクを上げないと立たない。**

### 対策：モジュールは分解せず「足す」だけ

必要トルクは概ね **τ ∝ 振子質量 × アーム長**。この2つだけを外付けで増やす。

| 改造 | 内容 | 効果 |
|---|---|---|
| **A. アーム延長バー** | DM-J4310 出力軸と QUBE アームハブの間にアルミバーを挟む。実効アーム長 85 → **300 mm** | モジュール無改造 |
| **B. 振子先端に錘** | 既存ロッド先端に **150 g** のカラーを追加（24 g → 174 g） | ネジ止めのみ、可逆 |

→ 必要トルク **約 0.154 N·m**。τ_c と同オーダーになり、摩擦補償と併せて実用域。

> 錘を増やすと不安定極は 10.7 → 9.0 rad/s（時定数 0.094 → 0.11 秒）とわずかに遅くなる。
> それでも短い振子なので速い部類。**制御周期は 500 Hz を狙う。**

> **数度のリミットサイクルが残ることは織り込む。** 減速機付きモータで Furuta を作る以上
> これは避けられず、摩擦補償とディザで緩和する。

---

## ハードウェア仕様

### 振子角センサ（QUBE 内蔵）

**US Digital E8P-512-118** シングルエンド光学エンコーダ。**4ピン（CON-MIC4）**。

| ピン | 信号 |
|---|---|
| 1 | +5 V |
| 2 | A 相 |
| 3 | GND |
| 4 | B 相 |

- 電源 **5.0 V（4.5〜5.5 V）**。3.3 V では動作しない
- 512 ライン → 4逓倍で **2048 カウント/回転 = 0.176°/カウント**
- **インデックス（Z相）なし** → 絶対角は取れない。振子を垂らした静止位置（α=π）でゼロ点を取る

⚠️ **ESP32 は 3.3 V I/O。A/B を直結すると壊れる。** レベルシフタか抵抗分圧（10k/20k）を必ず入れる。
⚠️ ESP32 の **PCNT（パルスカウンタ）**でハードウェア直交デコードする。取りこぼしが原理的に起きない。

### T_MAX を 1.0 N·m に下げる（重要）

現在 `T_MAX = 10 N·m` を 12bit で割るので **1 LSB = 4.9 mN·m**。
改造後の制御トルク 154 mN·m でも31段階しかなく、粗すぎる。

**`T_MAX`（RID 23）を 1.0 N·m に下げると分解能が 0.49 mN·m と10倍細かくなる。**
この振子には ±1.0 N·m で十分。

- `MotorScaling.TorqueMax` を同時に書き換える（**指令とフィードバック両方の解釈が変わる**）
- 変更後は必ずトルク校正をやり直す
- 変更は `SetControlMode` ツールと同じパラメータ書き込み機構で可能

---

## 発注・調達リスト

QUBE モジュールで軸受・エンコーダ・振子が揃ったため、当初リストから大幅に減った。

| 品目 | 用途 | 備考 |
|---|---|---|
| ESP32 ×1 | E8P 読み取り | PCNT が使えるもの |
| **レベルシフタ（5V↔3.3V）** | E8P → ESP32 | 双方向でなくてよい。抵抗分圧でも可 |
| **5V 電源** | E8P 用 | USB から取ってもよい |
| CON-MIC4 対応ケーブル | E8P 接続 | 現物のコネクタを確認してから |
| **アルミ板 t5（アーム延長バー用）** | 改造A | 300 mm。たわむと制御性能が直接落ちる |
| **DM-J4310 出力軸→バーのアダプタ** | 改造A | 3Dプリント可 |
| **錘（50 g / 100 g / 150 g）** | 改造B | 段階的に試すため複数 |
| M3/M4 ネジ・スペーサ類 | | |
| **非常停止スイッチ / ブレーカ** | 安全 | **必須** |

**不要になったもの**: AS5600、深溝玉軸受、φ6 アルミ丸棒、カーボンパイプ

---

# Week 1

## Day 1（月 8/10）✅ 完了

- [x] MIT モードへ変更＋フラッシュ保存、電源再投入して定着確認
- [x] `P_MAX` / `V_MAX` / `T_MAX` の実レジスタ値を照合 → 12.5 / 30.0 / 10.0 で一致。減速比 10:1
- [x] `Motor.TorqueCommand(torqueNm)` を追加（`kp=kd=0` を保証、単体テスト済み）
- [x] 逆駆動テスト（A/B 判定）→ 合格。寄生トルクなし
- [x] 静止摩擦の実測 → **τ_c = 0.147 N·m**

## Day 2（水 8/12）トルク校正とループ品質

- [x] **動摩擦・粘性の実測** → **τ_coulomb = 0.126 N·m / b = 0.027 N·m·s/rad**
      （正転 0.109 / 逆転 0.143 と非対称。摩擦補償は正逆で別係数を持たせること）
- [x] **制御周期の決定** → **300 Hz**（下表）。USB-CAN の往復遅延が約 2.3 ms で 436 Hz が上限
- [x] **`T_MAX` を 2.0 N·m に変更**し、`MotorScaling` を同期。既知の摩擦を測り直して整合を確認
- [x] `Motor.VerifyScaling()` を追加（トルクを出す前に必ず呼ぶ安全網）
- [ ] **トルクスケールの実測校正**（`TorqueCalibration --mode sweep`）← 実物の腕＋錘が必要
- [ ] **重力補償デモ**（`TorqueCalibration --mode float`）← 同上

### 実測した制御周期（2026/08/12、素のモータ、12秒間）

| 目標 | 実測平均 | 標準偏差 | 95%点 | 最大 | 取りこぼし |
|---|---|---|---|---|---|
| 200 Hz | 5.000 ms | 0.820 | 5.92 | 21.7 | 0% |
| **300 Hz ←採用** | **3.333 ms** | **0.563** | **3.75** | 21.8 | 0% |
| 400 Hz | 2.500 ms | 0.851 | 3.61 | 33.1 | 0% |
| 500 Hz | 2.294 ms ✗ | 1.158 | 3.52 | 38.3 | 0% |

**Day 3 のシミュレーションに入れる値**: 制御周期 3.33 ms、常用遅延 約 5 ms、
まれに 22 ms の外れ値。振子の時定数 約 100 ms に対して常用 5%、最悪 20%。

**完了条件**: 重力補償が成立。→ 実測値は上表のとおり揃っている。

## Day 3（木 8/13）シミュレーション環境 ✅ 完了（8/12 に前倒し実施）

- [x] SymPy で Furuta の運動方程式を導出 → [`../model/furuta_model.py`](../model/furuta_model.py)
- [x] `A = ∂f/∂x`, `B = ∂f/∂u` を平衡点で数値評価。**不安定極 13.0 rad/s（時定数 77 ms）**、可制御性ランク 4/4
- [x] LQR ゲイン計算（Bryson の法則）
- [x] 定常カルマンフィルタのゲイン設計
- [x] **実測の遅延・ジッタ・量子化・非対称クーロン摩擦を入れた非線形閉ループ検証**
- [x] 摩擦補償 `τ += τ_c·tanh(θ̇/ε)` を組み込み
- [x] 感度解析（摩擦補償の効き、アーム慣性 J_r）
- [x] ゲインを C# 定数として出力 → [`../model/FurutaGains.cs`](../model/FurutaGains.cs)

### 結果

| ケース | 結果 | リミットサイクル | 最大トルク |
|---|---|---|---|
| 初期傾き 3° | 成立 | 0.97° | 0.65 N·m |
| 初期傾き 6° | 成立 | 0.99° | 1.31 N·m |
| 初期傾き 10° | 成立 | 1.00° | 2.00 N·m（飽和） |
| 外乱 0.15 N·m × 30 ms | 成立 | 1.02° | 0.20 N·m |

**感度**: 摩擦補償の効きを 0〜1.1 倍に振っても成立（補償なしで 1.62°、完全補償で 0.91°）。
アーム慣性 J_r を 0.5〜3 倍に振っても成立。**推定値の誤差に対して頑健。**

> ⚠️ **シミュレーションのクーロン摩擦は tanh 近似**で、真の固着（速度ゼロでの停留）は
> 表現できていない。実機の摩擦はこれより厳しいはず。**摩擦補償は外さないこと。**

> ⚠️ アームが ±8° 程度をゆっくり往復する（摩擦由来の遅い振動）。
> 振子は 1° 以内に収まっているので許容だが、Day 7 の積分項で改善を試みる。

**完了条件**: 達成。

> **摩擦補償を最初から入れる理由**: 平衡点近傍では必要な制御トルクが定義上ゼロに近づくため、
> **どれだけ設計をスケールアップしても必ず摩擦の不感帯に入る**。摩擦を「上回る」ことは
> 原理的にできない。したがって摩擦補償は「余裕があればやる」ではなく設計の前提。

---

# Week 2

## Day 4（月 8/17）機構改造とエンコーダ配線

- [ ] **アーム延長バーの製作**（改造A）。DM 出力軸アダプタ＋300 mm バー。剛性を確保する
- [ ] QUBE モジュールを延長バー先端に固定
- [ ] **振子先端に錘を追加**（改造B）。**50 g から始めて手で回し、軸受の渋りが出ないか確認**
- [ ] E8P の 4ピンから配線を引き出し、レベルシフタ経由で ESP32 へ
- [ ] ESP32 の PCNT で直交デコード、USB シリアルへ垂れ流すファーム
- [ ] **スリップリングの有無を確認**（スイングアップで全周回転するため）

**完了条件**: 振子が滑らかに全周回転し、角度が正しく読める。
配線が絡まない。ガタつきはここで直す（後工程では直せない）。

## Day 5（水 8/19）パラメータ同定と統合

- [ ] アーム長・振子長・振子質量を**実測**（カタログ値を信用しない）
- [ ] 振子を下向きに吊るして微小振動 → 周期 T から `J₁_hat = m₁·g·l₁·(T/2π)²`
- [ ] 同じ自由振動の振幅の対数減衰から `b₁`
- [ ] 同定値をモデルに反映して LQR ゲイン再計算
- [ ] CAN ループ ＋ シリアル読み取り ＋ オブザーバ ＋ LQR を1本の固定周期ループに統合

**完了条件**: 全状態がリアルタイムに正しく表示される。トルク指令はまだ 0 のまま。

## Day 6（木 8/20）**実機で立てる**

1. [ ] **振子を手で持ったまま**制御を有効化し、傾けた向きにアームが正しく押すか確認 ← 符号ミスをここで殺す
2. [ ] トルクリミットを本来の 1/3 に絞って手を離す
3. [ ] 立ったらリミットを戻し、Q/R を調整（振子が落ちそう→α の重みを上げる／アームが暴れる→θ か R を上げる）

**完了条件**: 手で上向きにしてやれば自立する。

---

# Week 3

## Day 7（月 8/24）安定性の作り込み

- [ ] 摩擦補償の係数 τ_c と平滑化幅 ε を実機で追い込む（実装自体は Day 3 で済んでいる）
- [ ] **リミットサイクルが残るならディザを投入**（この構成では最も効く可能性が高い）
- [ ] **錘を 50 g → 100 g → 150 g と増やして安定性の変化を見る**（軸受の渋りに注意）
- [ ] 振子零点の精密校正（1°のズレがアームの定常ドリフトになる）
- [ ] θ の積分を5番目の状態として加えた拡大系 LQR でドリフト除去
- [ ] 外乱応答（指で突く）の確認

## Day 8（水 8/26）スイングアップ

- [ ] エネルギー整形則の実装
  - `E = ½·J₁_hat·α̇² + m₁·g·l₁·(cos α − 1)`（上向き静止で 0）
  - `u_accel = k·(E − 0)·sign(α̇·cos α)` ※飽和付き、符号はシミュレーションで確認
- [ ] 切り替え条件: **`|α| < 20°` かつ `|α̇|` が閾値以下**（角度だけで切り替えると吹き飛ぶ）
- [ ] 切り替え時にオブザーバ状態が連続であることを確認

## Day 9（木 8/27）予備日・仕上げ

- [ ] ログ・可視化、デモ用の起動手順整備
- [ ] **または Day 6〜8 の遅れの吸収**

---

# リスクと調整弁

**この構成の最大のリスクは摩擦。** 必要トルクと摩擦が同オーダーなので、
当初計画（振子 250 g / アーム 200 mm の自作）より余裕が小さい。
Day 3 のシミュレーションで摩擦を入れて立たない場合は、**Day 4 の時点で錘をさらに増やす**か、
アーム延長バーをより長くする（300 → 400 mm）ことで対応する。

**Day 6 が一発で通らない前提で組んである。** 通らなければ Day 7 を丸ごとデバッグに使い、
スイングアップを Day 8→9 にずらせば間に合う。

**それでも遅れた場合に切るのはスイングアップ。**
「手で上向きにセットすれば立って、突いても倒れない」までできていれば倒立振子としては完成。
**平衡制御を犠牲にしてスイングアップを優先することは絶対にしない。**

**Day 3 のシミュレーションを飛ばさない。** 実機デバッグ時に
「制御則が悪いのか実装が悪いのか」を切り分ける唯一の基準になる。

---

# 技術メモ

## DM モータ CAN プロトコル

**制御モードは送信 CAN ID のオフセットで決まる**

| モード | 送信 CAN ID |
|---|---|
| MIT | `0x000 + ID` |
| 位置速度 | `0x100 + ID` |
| 速度 | `0x200 + ID` |
| 力位混合 | `0x300 + ID` |

ただしファームによっては `CTRL_MODE` レジスタも合わせないと無反応
（1=MIT / 2=位置速度 / 3=速度 / 4=力位混合）。**現在は MIT(1) でフラッシュ保存済み。**

**特殊コマンド**（8バイト。モータIDあてに送る。`0x104` や `0x204` では受け付けられない）

| コマンド | データ |
|---|---|
| Enable | `FF FF FF FF FF FF FF FC` |
| Disable | `FF FF FF FF FF FF FF FD` |
| ゼロ点設定 | `FF FF FF FF FF FF FF FE` |
| エラークリア | `FF FF FF FF FF FF FF FB` |

**エラーコード**: 0=正常, 1=使能中, 8=過電圧, 9=低電圧, A=過電流, B=MOS過温, C=コイル過温, D=通信喪失, E=過負荷

**主なレジスタ（RID）**: 7=MST_ID, 8=CAN_ID, 9=TIMEOUT, 10=CTRL_MODE,
20=減速比, 21=P_MAX, 22=V_MAX, **23=T_MAX**

`Motor.ReadParameterSingle(RegisterId.TorqueMax)` で CAN 経由で直接読める。
公式上位機（達妙調試助手）は入手できていないが、これで代替できる。

## 状態推定（最大の技術的難所）

4状態のうち角速度2つは直接測れない。素朴な差分は量子化ノイズを増幅し、
それを潰すために LPF を強くかけると位相遅れで発振する。**失敗の主原因はここ。**

→ **線形モデルベースの定常カルマンフィルタ**を使う。観測は θ と α の2つ、モデルは `A`, `B` を流用。
モデルが速度を予測するため位相遅れが原理的に小さい。定常ゲインを事前計算すれば実行時は行列積2回。

簡易に各角度2状態のカルマンで済ませる場合も、**LPF のカットオフは振子の固有振動数（約 1.4 Hz）の
10倍以上**を確保する。

## 安全

- ホスト側ウォッチドッグ（一定時間指令が来なければトルク0）
- 角度リミット
- 温度監視（フィードバックの MOS・ロータ温度）
- エラーコードのパース（エラーで勝手に disable される）
- 例外・Ctrl+C・正常終了のいずれでも必ず Disable を送る（`try/finally`）

## 動かないときにまず疑う順序

1. **24V 電源が入っているか**（LED 赤＝失能 / 緑＝使能）
2. 送信 CAN ID のオフセット（MIT は `0x000+ID`）
3. `CTRL_MODE` がフラッシュに保存されているか
4. `MotorScaling` の定数とレジスタ実値の一致（**T_MAX 変更後は特に**）
